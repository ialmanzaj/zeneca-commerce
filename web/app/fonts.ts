import { Roboto_Mono, Inter, Manrope } from 'next/font/google';

export const roboto = Manrope({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-roboto-mono',
});

export const inter = Manrope({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});
